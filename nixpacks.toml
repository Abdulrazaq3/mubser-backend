[phases.setup]
nixPkgs = ["python310", "gcc", "zlib", "glib", "libglvnd"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
